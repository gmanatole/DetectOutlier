# `outlierdetect`

The top-level package is a convenience re-export surface. It gathers the data containers, physics helpers, model wrappers, runtime configuration, and training entry points into one import path so user code can stay short without hiding where the real logic lives.

```{automodule} outlierdetect
:members:
:undoc-members:
:private-members:
:show-inheritance:
```
