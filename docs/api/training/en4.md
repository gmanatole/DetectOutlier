# `outlierdetect.training.en4`

This module bridges clean EN4 profiles to synthetic training examples. It
mirrors the Argo training helpers, but its source reader walks the EN4 monthly
NetCDF files instead of Argo profile files.

```{automodule} outlierdetect.training.en4
:members:
:undoc-members:
:private-members:
:show-inheritance:
```
