# `outlierdetect.en4`

The EN4 reader turns monthly NetCDF files with multiple profiles into the same
profile objects used elsewhere in the package. That keeps the training path
source-agnostic while preserving the file-specific metadata needed by the
synthetic example builders.

```{automodule} outlierdetect.en4
:members:
:undoc-members:
:private-members:
:show-inheritance:
```
